# Pin loosely for now — tighten once the first CI run confirms versions that work together.
numpy>=1.26
playwright>=1.45
jinja2>=3.1
matplotlib>=3.8
requests>=2.31
google-api-python-client>=2.130
google-auth>=2.30
google-auth-oauthlib>=1.2
